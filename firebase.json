{
  "firestore": {
    "rules": "backend/firebase/firestore.rules",
    "indexes": "backend/firebase/firestore.indexes.json"
  },
  "storage": {
    "rules": "backend/firebase/storage.rules"
  },
  "database": {
    "rules": "backend/firebase/database.rules.json"
  }
}
